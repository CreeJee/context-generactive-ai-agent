{
  "name": "context-generactive-agent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vp run -r build",
    "ready": "vp check && vp run -r test && vp run -r build",
    "dev": "vp run agent#dev",
    "prepare": "vp config"
  },
  "devDependencies": {
    "@oxlint/plugins": "catalog:",
    "vite": "catalog:",
    "vite-plus": "catalog:"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "12.4.1",
      "onFail": "download"
    }
  },
  "engines": {
    "node": ">=26.8.2"
  }
}
